//let arrayLiteral = [[],[],[]];

//let rowIndex = 3;
//let columnCounter = 3;

//let array = new Array(rowIndex);

//for(let rowIndex = 0; rowIndex < array.length; rowIndex++){
   //for(let colCounter = 0; colCounter < array.length; colCounter++)
       //array[rowIndex] = new Array(colCounter).fill(0)
    //console.log(array);
//}

let arrayLiteral = [];

for(let index = 0; index < 7; index++) {
    arrayLiteral[index] = index;
}
    console.log(arrayLiteral)