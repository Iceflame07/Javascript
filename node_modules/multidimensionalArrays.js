//let arrayLiteral = [[],[],[]];

let rowIndex = 3;
let columnCounter = 3;

let array = new Array(row);

for(let rowIndex = 0; rowIndex < array.length; rowIndex++){
   for(let colCounter = 0; colCounter < array.length; colCounter++)
       array[rowIndex] = new Array(col).fill(0)
    console.log(array);
}